# MySQL connection
DATABASE_HOST=127.0.0.1
DATABASE_PORT=3306
DATABASE_USERNAME=dreamspace
DATABASE_PASSWORD=changeme
DATABASE_NAME=dreamspace

# Admin panel (/admin)
# Only used once, by `node scripts/seed.js`, to create the first admin_users
# row if the table is empty. After that, manage accounts at /admin/users.
ADMIN_EMAIL=admin@dreamspacerbg.com
ADMIN_PASSWORD=changeme
# Generate a real one for production: openssl rand -hex 32
ADMIN_SESSION_SECRET=dev-only-secret-change-in-production
