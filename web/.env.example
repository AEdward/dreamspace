NEXT_PUBLIC_STRAPI_URL=http://localhost:1337
# Only needed while the CMS runs on localhost during local development.
# Leave unset in production, where the CMS has a real public hostname.
ALLOW_LOCAL_CMS_IMAGES=true
